
sudo: true

branches:
        only:
                - dev

before_install:
        - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
        - sudo apt-get update -qq

install:
        - sudo apt-get install -qq gcc-4.9 g++-4.9 libsfml-dev liblua5.2-dev libluabind-dev

script:
        - scons

compiler:
        - gcc

